import{spawn}from'node:child_process';import fs from'node:fs';import path from'node:path';
const origin='http://localhost:5173',profile=path.resolve('.browser-smoke-profile'),chrome='C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe';fs.mkdirSync(profile,{recursive:true});
const created=await fetch(`${origin}/api/rooms`,{method:'POST',headers:{'Content-Type':'application/json'},body:'{}'}).then(r=>r.json()),roomId=created.roomId,session=await fetch(`${origin}/api/session`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({roomId,name:'Browser QA'})}).then(r=>r.json());
const browser=spawn(chrome,['--headless=new','--disable-gpu','--no-first-run','--no-default-browser-check','--remote-debugging-port=9224',`--user-data-dir=${profile}`,'about:blank'],{stdio:'ignore'});let socket;try{let target;for(let i=0;i<50&&!target;i++){await new Promise(r=>setTimeout(r,100));try{target=(await fetch('http://127.0.0.1:9224/json/new?about:blank',{method:'PUT'}).then(r=>r.json()))}catch{}}if(!target)throw new Error('Chrome debugging endpoint did not start');socket=new WebSocket(target.webSocketDebuggerUrl);await new Promise((resolve,reject)=>{socket.addEventListener('open',resolve,{once:true});socket.addEventListener('error',reject,{once:true})});let id=0;const pending=new Map(),call=(method,params={})=>new Promise((resolve,reject)=>{const callId=++id;pending.set(callId,{resolve,reject});socket.send(JSON.stringify({id:callId,method,params}))});socket.addEventListener('message',event=>{const message=JSON.parse(event.data);if(message.id&&pending.has(message.id)){const task=pending.get(message.id);pending.delete(message.id);message.error?task.reject(new Error(message.error.message)):task.resolve(message.result)}});await call('Page.enable');await call('Runtime.enable');await call('Page.navigate',{url:origin});await new Promise(r=>setTimeout(r,500));await call('Runtime.evaluate',{expression:`localStorage.setItem('cocreate-session-${roomId}',${JSON.stringify(session.token)});localStorage.setItem('cocreate-name','Browser QA')`});await call('Page.navigate',{url:`${origin}/r/${roomId}`});await new Promise(r=>setTimeout(r,1300));const evaluate=async expression=>(await call('Runtime.evaluate',{expression,returnByValue:true})).result.value;let text=await evaluate('document.body.innerText');if(!text.includes('API connections')||!text.includes('Generation is paused'))throw new Error('Workspace connection controls did not render');await evaluate(`[...document.querySelectorAll('button')].find(b=>b.textContent.includes('API connections')).click()`);await new Promise(r=>setTimeout(r,250));text=await evaluate('document.body.innerText');if(!text.includes('Connect the workspace builder')||!text.includes('API key')||text.includes('Try demo'))throw new Error('Owner API panel is incomplete or still exposes demo UI');await evaluate(`[...document.querySelectorAll('button')].find(b=>b.getAttribute('aria-label')==='Close API connections').click()`);await evaluate(`[...document.querySelectorAll('button')].find(b=>b.textContent.includes('Product')).click()`);await new Promise(r=>setTimeout(r,200));text=await evaluate('document.body.innerText');if(!text.includes('Connect an AI model to generate your app.'))throw new Error('Disconnected Product state is incorrect');await call('Emulation.setDeviceMetricsOverride',{width:390,height:844,deviceScaleFactor:1,mobile:true});await new Promise(r=>setTimeout(r,200));const metrics=await evaluate('({width:document.documentElement.scrollWidth,viewport:innerWidth,text:document.body.innerText})');if(metrics.width>metrics.viewport||!metrics.text.includes('Product'))throw new Error('Mobile workspace overflow or navigation failure');console.log(JSON.stringify({roomId,ownerPanel:true,productEmptyState:true,mobileWidth:metrics.width,viewport:metrics.viewport}))}finally{socket?.close();browser.kill()}
