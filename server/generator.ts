import{readFile}from'node:fs/promises';import{createRequire}from'node:module';import{build}from'esbuild';import type{AIFormat,AIProvider,ChangeKind,ProductSource,Requirement,SharedRequirement}from'../src/types.js';import{applyOperations,budgetProjectFiles,type ProjectFile,type ProjectPlan}from'./project.js';import{demoExtract,demoOrchestrate}from'./demo.js';import{INTERPRETATION_CLASSIFIER_VERSION,normalizeInterpretation}from'./requirements.js';import{discoverModels,generateStructured,generateText,providerDefaults,validateProviderUrl,type ProviderConfig,type Usage}from'./providers.js';
export type AgentChange={seq:number;kind:ChangeKind;before:string;after:string};export type AIConfig={mode:'demo'|'openai';apiKey?:string;model:string;baseUrl?:string;apiFormat?:AIFormat;provider?:AIProvider;maxInputTokens?:number;maxOutputTokens?:number;specialtyInstruction?:string;presetVersion?:string;pricingVersion?:string};
const MAX_SOURCE=30_000,banned=/\b(fetch|XMLHttpRequest|WebSocket|EventSource|localStorage|sessionStorage|indexedDB|document\.cookie|window\.(parent|top|opener)|eval|Function)\b/;
export function validateSource(source:ProductSource){if(!source.app||source.app.length>MAX_SOURCE||source.css.length>MAX_SOURCE)throw new Error('Generated source exceeded the allowed size.');if(banned.test(source.app))throw new Error('Generated source requested a blocked browser capability.');if(/\bimport\s|\brequire\s*\(/.test(source.app))throw new Error('Generated source used an unapproved dependency.');return source}
export async function bundleSource(source:ProductSource){validateSource(source);const dependencyPlugin={name:'cocreate-dependencies',setup(api:any){api.onResolve({filter:/.*/},(args:any)=>{if(args.kind==='entry-point')return;const importer=args.importer&&!args.importer.startsWith('<')?args.importer:import.meta.url;return{path:createRequire(importer).resolve(args.path),namespace:'cocreate-dependency'}});api.onLoad({filter:/.*/,namespace:'cocreate-dependency'},async(args:any)=>({contents:await readFile(args.path,'utf8'),loader:args.path.endsWith('.json')?'json':'js'}))}};const entry=`import React from 'react';import{createRoot}from'react-dom/client';globalThis.React=React;${source.app};createRoot(document.getElementById('root')).render(React.createElement(App));`;const result=await build({stdin:{contents:entry,loader:'tsx'},plugins:[dependencyPlugin],define:{'process.env.NODE_ENV':'"production"'},bundle:true,write:false,format:'iife',platform:'browser',target:'es2022',logLevel:'silent'});return result.outputFiles[0].text}
const boundedInput=(input:string)=>{if(Buffer.byteLength(input)>140_000)throw new Error('Prepared model context exceeds the safe request budget.');return input};
export function validateBaseUrl(value:string,provider:AIProvider='custom'){return validateProviderUrl(value,provider)}
const providerConfig=(apiKey:string,baseUrl:string,apiFormat:AIFormat,provider:AIProvider):ProviderConfig=>({provider,apiKey:apiKey||undefined,baseUrl:validateProviderUrl(baseUrl||providerDefaults[provider].baseUrl,provider),apiFormat});
export type ModelResult<T>={value:T;usage:Usage};
export async function callOpenAI<T=unknown>(apiKey:string,model:string,instructions:string,input:string,schema?:object,baseUrl='https://api.openai.com/v1',apiFormat:AIFormat='responses',provider:AIProvider='openai',signal?:AbortSignal,maxOutputTokens?:number):Promise<ModelResult<T>>{const request={model,instructions,input:boundedInput(input),schema:schema as Record<string,unknown>|undefined,maxOutputTokens:maxOutputTokens??(schema?6_000:256),timeoutMs:schema?150_000:30_000,signal};if(schema){const result=await generateStructured(providerConfig(apiKey,baseUrl,apiFormat,provider),request);return{value:result.value as T,usage:result.usage}}const result=await generateText(providerConfig(apiKey,baseUrl,apiFormat,provider),request);return{value:result.text as T,usage:result.usage}}
export async function testOpenAIConnection(apiKey:string,model:string,baseUrl='https://api.openai.com/v1',apiFormat:AIFormat='responses',provider:AIProvider='openai'){if(!model.trim())throw new Error('Enter a model ID.');const result=await generateText(providerConfig(apiKey,baseUrl,apiFormat,provider),{model:model.trim(),instructions:'Return only the word OK.',input:'Connection test',maxOutputTokens:64,timeoutMs:25_000});return{ok:true,model:model.trim(),provider,apiFormat,usage:result.usage}}
export async function listProviderModels(apiKey:string,baseUrl='https://api.openai.com/v1',provider:AIProvider='openai',apiFormat:AIFormat='responses'){return discoverModels(providerConfig(apiKey,baseUrl,apiFormat,provider))}
const strings=(maxItems:number)=>({type:'array',items:{type:'string'},maxItems}),classification={type:'string',enum:['proposal','question','explicit_request','decision','ambiguity']},intentSchema={type:'object',additionalProperties:false,required:['text','category','classification','rationale','sourcePassage','affectedRequirementIds'],properties:{text:{type:'string'},category:{type:'string',enum:['goal','feature','design','constraint','question','withdrawal']},classification,rationale:{type:'string'},sourcePassage:{type:'string'},affectedRequirementIds:strings(12)}};export const reqSchema={type:'object',additionalProperties:false,required:['goals','features','design','constraints','questions','additions','modifications','withdrawals','classification','affectedRequirementIds','sourcePassages','intents'],properties:{goals:strings(8),features:strings(12),design:strings(8),constraints:strings(8),questions:strings(6),additions:strings(10),modifications:strings(10),withdrawals:strings(10),classification,affectedRequirementIds:strings(12),sourcePassages:strings(8),intents:{type:'array',maxItems:16,items:intentSchema}}};const productSchema={type:'object',additionalProperties:false,required:['app','css','summary','decisions','conflicts'],properties:{app:{type:'string'},css:{type:'string'},summary:{type:'string'},decisions:strings(8),conflicts:strings(8)}};export const projectSchema={type:'object',additionalProperties:false,required:['operations','summary','decisions','conflicts','specification'],properties:{operations:{type:'array',minItems:1,maxItems:20,items:{type:'object',additionalProperties:false,required:['type','path','content'],properties:{type:{type:'string',enum:['write','delete']},path:{type:'string'},content:{type:'string'}}}},summary:{type:'string'},decisions:strings(8),conflicts:strings(8),specification:{type:'object',additionalProperties:false,required:['agreed','proposed','questions'],properties:{agreed:strings(16),proposed:strings(12),questions:strings(8)}}}};
const compactText=(value:string,limit:number)=>value.length<=limit?value:`${value.slice(0,Math.floor(limit/2))}\n…\n${value.slice(-Math.ceil(limit/2))}`;
const compactRequirement=(requirement:Requirement)=>{const compact=(items:string[],limit:number)=>items.slice(0,limit).map(item=>compactText(item,360));return{participantName:requirement.participantName,goals:compact(requirement.goals,8),features:compact(requirement.features,12),design:compact(requirement.design,8),constraints:compact(requirement.constraints,8),questions:compact(requirement.questions,6),additions:compact(requirement.additions,10),modifications:compact(requirement.modifications,10),withdrawals:compact(requirement.withdrawals,10),classification:requirement.classification,affectedRequirementIds:requirement.affectedRequirementIds,sourceRevision:requirement.sourceRevision,revision:requirement.revision}};
const compactSharedRequirement=(requirement:SharedRequirement)=>({id:requirement.id,revision:requirement.revision,category:requirement.category,description:compactText(requirement.description,500),acceptanceCriteria:requirement.acceptanceCriteria.slice(0,6).map(item=>compactText(item,360)),status:requirement.status,sources:requirement.sources.slice(-6).map(source=>({participantId:source.participantId,participantName:source.participantName,documentRevision:source.documentRevision,editSeqs:source.editSeqs.slice(-12)}))});
export async function extractRequirement(config:AIConfig,participantId:string,participantName:string,changes:AgentChange[],context:string,previous:Requirement|undefined,revision:number,signal?:AbortSignal):Promise<ModelResult<Requirement>>{if(config.mode==='demo')return{value:normalizeInterpretation(demoExtract(participantId,participantName,changes,context,previous,revision)),usage:{}};if(!config.apiKey)throw new Error('AI connection is unavailable. Ask the workspace owner to reconnect it.');const compactChanges=changes.slice(-24).map(change=>({seq:change.seq,kind:change.kind,before:compactText(change.before,400),after:compactText(change.after,400)})),emphasis=config.specialtyInstruction?` Task emphasis: ${config.specialtyInstruction} Explicit accepted requirements always take precedence over this emphasis.`:'',result=await callOpenAI<any>(config.apiKey,config.model,'Maintain a compact evolving interpretation of this collaborator’s authenticated contribution. Emit one intent record per distinct request, proposal, question, decision, or withdrawal. explicit_request means the user directs creation/change/removal/implementation, including polite “Can you…”, “I want…”, spelling mistakes, and requests missing details. proposal means an uncommitted option such as “maybe”, “one idea”, or “what if”. question asks for information or a decision; a polite implementation request is not a question. decision records a settled authorized direction. ambiguity is only for genuinely indeterminate intent. Mixed sentences must remain separate intents. Quoted examples and hypotheticals are not live requests. Preserve negation and explicit withdrawals; deletion alone is not withdrawal. Give each intent a short user-facing rationale and exact source passage. Use affectedRequirementIds only for known IDs. Keep the legacy summary arrays consistent with the intents. Use shared canvas only as context and never attribute teammates’ text to this person. Return no private reasoning.'+emphasis,JSON.stringify({classifierVersion:INTERPRETATION_CLASSIFIER_VERSION,participantName,authenticatedChanges:compactChanges,previousContributionSummary:previous?compactRequirement(previous):null,sharedBrainstormCanvas:compactText(context,6_000)}),reqSchema,config.baseUrl,config.apiFormat,config.provider,signal,config.maxOutputTokens??2_400),stamped={...result.value,id:crypto.randomUUID(),participantId,participantName,sourceRevision:revision,sourceEditSeqs:compactChanges.map(change=>change.seq),sourcePassages:(result.value.sourcePassages||[]).slice(0,8),revision,createdAt:new Date().toISOString(),classifierVersion:INTERPRETATION_CLASSIFIER_VERSION};return{value:normalizeInterpretation(stamped),usage:result.usage}}
export async function generateProduct(config:AIConfig,requirements:Requirement[],previous?:ProductSource,sharedCanvas='',signal?:AbortSignal):Promise<ModelResult<ProductSource>>{if(config.mode==='demo')return{value:demoOrchestrate(requirements),usage:{}};if(!config.apiKey)throw new Error('AI connection is unavailable. Ask the workspace owner to reconnect it.');const result=await callOpenAI<ProductSource>(config.apiKey,config.model,'You are the room’s single product builder. The team is collaboratively brainstorming and co-writing in one shared canvas—not tracking personal progress. Synthesize the whole current canvas with the attributed contribution summaries into one coherent working product. Incrementally update the previous successful React frontend. Resolve overlapping ideas constructively; withdrawn ideas must not remain unless another collaborator still supports them. Preserve unaffected behavior. App must declare exactly export default function App(), use globalThis.React for hooks, and contain no imports, network or browser-storage access, eval, or parent-window access. Return CSS separately and no private reasoning.',JSON.stringify({sharedBrainstormCanvas:compactText(sharedCanvas,8_000),currentAttributedContributions:requirements.map(compactRequirement),previousSuccessfulProduct:previous?{...previous,app:compactText(previous.app,18_000),css:compactText(previous.css,8_000)}:null}),productSchema,config.baseUrl,config.apiFormat,config.provider,signal,6_000);return{value:validateSource(result.value),usage:result.usage}}
const mergeUsage=(first:Usage,second:Usage):Usage=>{const sum=(key:keyof Usage)=>first[key]===undefined&&second[key]===undefined?undefined:(typeof first[key]==='number'?first[key] as number:0)+(typeof second[key]==='number'?second[key] as number:0),inputTokens=sum('inputTokens'),cachedInputTokens=sum('cachedInputTokens'),cacheWriteTokens=sum('cacheWriteTokens'),outputTokens=sum('outputTokens'),reasoningTokens=sum('reasoningTokens'),rateLimitRemaining=second.rateLimitRemaining??first.rateLimitRemaining,rateLimitReset=second.rateLimitReset??first.rateLimitReset,reasoningIncludedInOutput=first.reasoningIncludedInOutput||second.reasoningIncludedInOutput;return{...(inputTokens!==undefined?{inputTokens}:{}),...(cachedInputTokens!==undefined?{cachedInputTokens}:{}),...(cacheWriteTokens!==undefined?{cacheWriteTokens}:{}),...(outputTokens!==undefined?{outputTokens}:{}),...(reasoningTokens!==undefined?{reasoningTokens}:{}),...(reasoningIncludedInOutput!==undefined?{reasoningIncludedInOutput}:{}),rateLimitRemaining,rateLimitReset}};
export async function generateProjectPlan(config:AIConfig,requirements:SharedRequirement[],currentFiles:ProjectFile[]|undefined,compilerError?:string,signal?:AbortSignal):Promise<ModelResult<ProjectPlan>>{
  if(!config.apiKey)throw new Error('AI connection is unavailable. Ask the workspace owner to reconnect it.');
  const project=budgetProjectFiles(currentFiles,36_000),emphasis=config.specialtyInstruction?` Task emphasis: ${config.specialtyInstruction} Explicit accepted requirements always take precedence over this emphasis.`:'',instructions='You are the room’s one serialized coding agent. Maintain a real small React and TypeScript frontend from the accepted shared specification. Return validated file operations, never shell commands. Every operation must include type, path, and content; use an empty content string for delete operations. You may write or delete project-relative files only under src/. Use only React, react-dom/client, relative modules, CSS, JSON, and the provided localStorage interface. Do not use network requests, dynamic imports, browser database APIs, parent-window access, or external assets. Preserve unaffected working features. Implement only the accepted requirements supplied here; proposals, questions, ambiguous notes, and raw canvas text are deliberately excluded. Resolve reversible details with sensible defaults. src/main.tsx must remain the entrypoint. When a validation or compiler error is supplied, repair it without discarding earlier behavior.'+emphasis,request=(repairError?:string)=>callOpenAI<ProjectPlan>(config.apiKey!,config.model,instructions,JSON.stringify({acceptedRequirements:requirements.map(compactSharedRequirement),currentProject:project.files,omittedUnchangedFiles:project.omitted,compilerError:[compilerError,repairError].filter(Boolean).join('\n').slice(0,3_000)||null}),projectSchema,config.baseUrl,config.apiFormat,config.provider,signal,config.maxOutputTokens??6_000),first=await request();
  try{applyOperations(currentFiles,first.value.operations);return first}catch(error){
    if(signal?.aborted)throw error;
    const message=error instanceof Error?error.message:String(error),repaired=await request(`Project operation validation failed: ${message}`);
    try{applyOperations(currentFiles,repaired.value.operations)}catch(repairError){const repairMessage=repairError instanceof Error?repairError.message:String(repairError);throw new Error(`Builder returned invalid project operations after repair. ${repairMessage}`)}
    return{value:repaired.value,usage:mergeUsage(first.usage,repaired.usage)};
  }
}
export const previewHtml=(bundle:string,css:string)=>`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; script-src 'unsafe-inline'; style-src 'unsafe-inline'; img-src data:; font-src data:; connect-src 'none'; form-action 'none'; base-uri 'none'"><style>${css.replace(/<\/style/gi,'<\\/style')}</style></head><body><div id="root"></div><script>(function(){var values={},started=false;Object.defineProperty(globalThis,'localStorage',{value:{get length(){return Object.keys(values).length},key:function(i){return Object.keys(values)[i]||null},getItem:function(k){return Object.prototype.hasOwnProperty.call(values,k)?values[k]:null},setItem:function(k,v){values[String(k)]=String(v);parent.postMessage({type:'cocreate-storage-set',key:String(k),value:String(v)},'*')},removeItem:function(k){delete values[String(k)];parent.postMessage({type:'cocreate-storage-remove',key:String(k)},'*')},clear:function(){values={};parent.postMessage({type:'cocreate-storage-clear'},'*')}}});function start(){if(started)return;started=true;try{globalThis.__cocreateRun()}catch(error){parent.postMessage({type:'cocreate-preview-error',message:String(error&&error.message||error)},'*')}}addEventListener('message',function(event){if(event.source!==parent||event.data&&event.data.type!=='cocreate-storage-init')return;values=event.data.values||{};start()});addEventListener('error',function(e){parent.postMessage({type:'cocreate-preview-error',message:e.message},'*')});parent.postMessage({type:'cocreate-storage-ready'},'*');setTimeout(start,700)})();</script><script>globalThis.__cocreateRun=function(){${bundle.replace(/<\/script/gi,'<\\/script')}}</script></body></html>`;
